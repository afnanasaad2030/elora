# متجر ELORA — دليل الإعداد

هذا الدليل لك أنت (الشخص التقني). للتشغيل اليومي هناك ملف منفصل: **`دليل-الاستخدام-اليومي.md`** — سلّمه لمن سيدير المتجر.

---

## الفكرة في سطرين

المتجر صفحة واحدة تُنشر مجانًا على GitHub Pages. من يديره **لا يحتاج أي برنامج**: يفتح `admin.html` من جواله، يضغط «إضافة»، يختار الصور، وتظهر في المتجر خلال دقيقة.

لوحة التحكم ترفع الملفات مباشرة إلى المستودع عبر واجهة GitHub، ثم يعيد GitHub Actions بناء الموقع تلقائيًا.

---

> **الروابط في هذا الدليل تحتوي `USERNAME` — استبدلها باسم حسابك الحقيقي على GitHub.**
> ولن تعمل قبل إتمام القسم 3 (لا يوجد موقع قبل نشره).

---

## 1) بياناتك

**الأسهل:** بعد النشر، اضبطها من لوحة التحكم مباشرة — زر **⚙** فيها يعدّل اسم المتجر ورقم الواتساب ونص الرسالة وانستقرام ولون الهوية، ويحفظها في `site.config.json` تلقائيًا.

**أو** عدّل الملف يدويًا قبل النشر:

```json
{
  "brandName": "ELORA",
  "tagline": "إكسسوارات مصنوعة يدويًا بحب",
  "logo": "assets/logo.png",
  "whatsapp": "967771234567",
  "whatsappMessage": "السلام عليكم، أرغب بطلب هذا المنتج من ELORA: ",
  "instagram": "",
  "accent": "#ba8490",
  "defaultTheme": "light"
}
```

**رقم الواتساب إلزامي** — بصيغة دولية بدون `+` وبدون الأصفار الأولى.
مثال يمني `967771234567` · سعودي `966501234567`

أي حقل تتركه `""` يختفي زره من الموقع تلقائيًا.

---

## 2) المتطلبات على جهازك

```bash
pip install pillow
```

ثم [Git](https://git-scm.com/download/win) إن لم يكن مثبّتًا. (هذه لك وحدك — من يدير المتجر لا يحتاج شيئًا منها.)

---

## 3) النشر لأول مرة

1. أنشئ مستودعًا على [github.com](https://github.com) باسم `elora`، واجعله **Public**.
2. من مجلد `photo-store` نفّذ (غيّر `USERNAME`):

```bash
git init && git branch -M main && git lfs install && git add -A && git commit -m "متجر ELORA" && git remote add origin https://github.com/USERNAME/elora.git && git push -u origin main
```

3. في المستودع: **Settings → Pages → Source** اختر **GitHub Actions**.

بعد دقيقة يصبح المتجر على:

```
https://USERNAME.github.io/elora/
```

ولوحة التحكم على نفس الرابط + `/admin.html`

---

## 4) إنشاء مفتاح الوصول للوحة التحكم

من GitHub: **صورتك ← Settings ← Developer settings ← Personal access tokens ← Fine-grained tokens ← Generate new token**

| الحقل | القيمة |
|---|---|
| Token name | `ELORA Admin` |
| Expiration | سنة (دوّن التاريخ — يجب تجديده بعدها) |
| Repository access | **Only select repositories** ← اختر `elora` |
| Permissions → Repository permissions → **Contents** | **Read and write** |

اضغط **Generate token** وانسخ المفتاح فورًا (يظهر مرة واحدة فقط).

ثم افتح `admin.html` **على جوال من سيدير المتجر**، وأدخل:
- اسم حسابك على GitHub
- اسم المستودع: `elora`
- المفتاح

يُحفظ المفتاح في متصفح ذلك الجهاز فقط. أضف الصفحة إلى الشاشة الرئيسية لتصبح كالتطبيق.

### ملاحظات أمان

- المفتاح مقيّد بمستودع الصور وحده وبصلاحية المحتوى فقط — لا يصل لأي شيء آخر في حسابك.
- صفحة `admin.html` عامة على الإنترنت لكنها **بلا فائدة بدون المفتاح**، وهي محجوبة عن محركات البحث.
- عند فقدان الجهاز: احذف المفتاح من صفحة Tokens في GitHub وأصدر غيره.
- زر **«فصل هذا الجهاز»** داخل اللوحة يمسح المفتاح من الجوال.

---

## 5) طريقتان للإدارة

### أ. لوحة التحكم (لمن يدير المتجر يوميًا)
من الجوال، بلا برامج: إضافة، تعديل، حذف، وضبط الإعدادات (⚙) بما فيها رقم الواتساب.
شرحها كامل في `دليل-الاستخدام-اليومي.md`.

### ب. من جهازك مباشرة (لك، عند الإضافة بالجملة)

| الملف | الوظيفة |
|---|---|
| `1-إضافة-صور.bat` | يفتح مجلد `media` — اسحب الصور إلى مجلد التصنيف |
| `2-معاينة-المتجر.bat` | يبني المتجر ويفتحه على `localhost:8000` |
| `3-نشر-على-الإنترنت.bat` | يبني وينشر بضغطة واحدة |

**اسم الملف = اسم المنتج، واسم المجلد = التصنيف.** الطريقتان متوافقتان تمامًا ويمكن استخدامهما بالتناوب.

> `data.json` و `media/_thumbs` تُبنى تلقائيًا — لا تعدّلهما يدويًا.
> مجلد يبدأ اسمه بـ `_` يتجاهله الموقع (مفيد لإخفاء منتجات دون حذفها).

---

## 6) الباركود لكرت العمل

```bash
pip install "qrcode[pil]"
```

```bash
python tools/make_qr.py https://USERNAME.github.io/elora/
```

تجد في مجلد `qr/` ملف **`qr.svg`** للمطبعة (يُكبَّر بلا فقدان جودة) و`qr.png` للمعاينة.

### ⚠️ الأهم في المشروع كله

الباركود المطبوع **لا يُعدَّل**. لو طبعت 1000 كرت ثم انتقلت لمنصة أخرى تصبح كلها بلا قيمة.

**الحل:** اشترِ نطاقًا (~12$ سنويًا) مثل `elora.store`، اربطه بـ GitHub Pages، واجعل الباركود يشير إليه. لو غيّرت المنصة لاحقًا تغيّر وجهة النطاق فقط وتبقى كل الكروت تعمل.

### قواعد الطباعة
- **لا يقل عن 2×2 سم**.
- اترك الهامش الأبيض حوله — بدونه لن تقرأه الكاميرا.
- أسود على أبيض، لا فوق صورة.
- اكتب تحته: **«امسح لمشاهدة التشكيلة»**.
- **اطبع كرتًا واحدًا وامسحه بجوالك قبل طباعة الكمية.**

---

## 7) حل المشاكل

| المشكلة | الحل |
|---|---|
| اللوحة: «المفتاح غير صحيح» | انتهت صلاحيته. أصدر مفتاحًا جديدًا وألصقه. |
| اللوحة: «لم يُعثر على المستودع» | تأكد من اسم الحساب واسم المستودع وأن المفتاح يشمله. |
| اللوحة: «تعذّر النشر» | انقطاع إنترنت، أو فيديو أكبر من 24 ميجابايت. |
| التحديث لا يظهر | انتظر دقيقة، ثم راجع تبويب **Actions** في المستودع. |
| الصفحة تفتح فارغة محليًا | لا تفتح `index.html` بالنقر المزدوج — استخدم `2-معاينة-المتجر.bat`. |
| المتصفح يعرض تصميمًا أو شعارًا قديمًا | لا يحدث بعد الآن: `build.py` يلحق رقم إصدار بروابط الملفات في كل بناء، فيجبر المتصفح على جلب النسخة الجديدة. إن ظهر مرة، اضغط `Ctrl+F5`. |
| `python is not recognized` | ثبّت Python وفعّل **Add to PATH**. |
| الصور بطيئة | `pip install pillow` ثم أعد النشر. |
| `file is too large` | ملف أكبر من 100 ميجابايت. اقتصّ الفيديو. |

---

## هوية ELORA البصرية

الألوان مستخرجة آليًا من الشعار نفسه، فلا تخمين فيها:

| اللون | الكود | الاستخدام |
|---|---|---|
| وردي | `#ba8490` | اللون الأساسي: الأزرار، التصنيف المختار، وسم «جديد» |
| زيتي | `#9ca890` | لون مساند من سطر Handmade Accessories |
| خلفية | `#fbf8f6` | أبيض دافئ مائل للوردي |
| نص | `#2c2522` | فحمي دافئ |

الخط: **IBM Plex Sans Arabic** — خط عربي عصري نظيف.
الشبكة: بطاقات بنسبة ثابتة 4:5 لمظهر متجر منتظم، لا معرض صور متفاوت.

ملفات الهوية في `assets/`:

| الملف | الوظيفة |
|---|---|
| `logo.png` | الشعار بخلفية شفافة (1600×606) — يعمل على الفاتح والداكن |
| `icon.png` | أيقونة التبويب والشاشة الرئيسية (حرف E على مربع وردي) |
| `og-image.png` | صورة المشاركة على واتساب وفيسبوك (1200×630) |

لتغيير الشعار لاحقًا: استبدل `assets/logo.png` بنسخة شفافة بنفس النسبة تقريبًا.

---

## خريطة الملفات

```
photo-store/
├─ index.html               المتجر
├─ admin.html               لوحة التحكم
├─ site.config.json         ← بياناتك
├─ media/                   الصور والفيديوهات (التصنيف = اسم المجلد)
├─ assets/                  التصميم والشعار
├─ tools/build.py           محرّك التحديث
├─ tools/make_qr.py         مولّد الباركود
├─ .github/workflows/       النشر التلقائي
├─ 1-إضافة-صور.bat
├─ 2-معاينة-المتجر.bat
├─ 3-نشر-على-الإنترنت.bat
├─ دليل-ELORA.md            هذا الملف
└─ دليل-الاستخدام-اليومي.md  ← سلّمه لمن يدير المتجر
```

---

## المحتوى الحالي

**45 منتجًا في 9 تصنيفات**، مستوردة من مجلد صورك ومسمّاة بالعربية:

| التصنيف | العدد |
|---|---|
| حقائب | 10 |
| أساور | 8 |
| أطقم | 6 |
| سلاسل مفاتيح | 6 |
| عقود | 4 |
| أحزمة | 3 |
| أقراط | 3 |
| إكسسوارات متنوعة | 3 |
| إكسسوارات اليد | 2 |

راجع الأسماء في `media/` وعدّل ما لا يعجبك — إعادة تسمية الملف تكفي.
